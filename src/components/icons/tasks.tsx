import clsx from 'clsx'
import React from 'react'

type Props = { selected: boolean }

const CRM = ({ selected }: Props) => {
  return (
    <img src='/icons/crm.png' className='h-3'/>
  )
}

export default CRM
